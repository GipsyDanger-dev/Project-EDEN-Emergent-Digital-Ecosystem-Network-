{
  "name": "@eden/engine",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "test": "vitest",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@eden/ai": "workspace:*",
    "@eden/citizen": "workspace:*",
    "@eden/core": "workspace:*",
    "@eden/history": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  }
}
