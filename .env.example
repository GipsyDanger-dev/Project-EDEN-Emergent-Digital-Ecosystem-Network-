# Server-authoritative simulation
PORT=3000
EDEN_SEED=7331
EDEN_CITIZENS=4
EDEN_TICK_RATE=1000

# Optional durable runtime persistence. Without this, the server uses an in-memory store.
DATABASE_URL=

# Browser endpoints (defaults target port 3000 on the current hostname)
NEXT_PUBLIC_EDEN_API_URL=http://localhost:3000
NEXT_PUBLIC_EDEN_WS_URL=ws://localhost:3000/ws
